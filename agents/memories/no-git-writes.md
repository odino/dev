## Git Write Operations

Never perform git write operations (add, commit, push, amend, rebase, reset, or any other operation that modifies git history or the index) unless explicitly instructed to do so by the user.
